// const jwt = require("jsonwebtoken");
// module.exports = (req, res, next) => {
//   if (req.cookies.auth != null) {
//     req.body.id = jwt.decode(req.cookies.auth)["_id"];
//   }
//   next();
// };
